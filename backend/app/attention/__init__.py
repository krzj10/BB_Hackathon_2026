"""A06 attention pipeline pieces owned by Stream A: deterministic rules
(attention/rules.py) and Gmail ingestion (attention/ingest.py). B's
AttentionEngine/Focus/Decisions consume these; they are NOT reimplemented."""
